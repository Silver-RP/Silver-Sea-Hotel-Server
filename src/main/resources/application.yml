spring:
  datasource:
    url: jdbc:mysql://localhost:3306/Silver-Sea
    username: rp
    password: Sauke123@Provip
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
      format_sql: true
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect


server:
  port: 8080
  servlet:
    multipart:
      enabled: true
      max-file-size: 5MB
      max-request-size: 5MB
      file-size-threshold: 2KB
#    context-path: /myapp   # Define your servlet context path here
#    session:
#      timeout: 30m




